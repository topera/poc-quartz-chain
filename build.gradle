// tested with gradle 6.0.1
apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

test{
    testLogging {
        showStandardStreams = true
    }
    dependsOn cleanTest
}

defaultTasks 'test'